version: 2
updates:
  # One root entry: pnpm workspaces are resolved from here so the root
  # pnpm-lock.yaml is updated alongside each app's package.json.
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      minor-and-patch:
        update-types: ["minor", "patch"]
